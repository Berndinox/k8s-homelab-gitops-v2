---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: firewall
  namespace: network
  labels:
    app: firewall
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: firewall
  template:
    metadata:
      labels:
        app: firewall
      annotations:
        k8s.v1.cni.cncf.io/networks: |
          [
            {"name": "trunk-bond0", "interface": "eth1"}
          ]
        # Kata: tcfilter leitet Traffic von macvlan via TC-Regeln in die VM
        io.katacontainers.config.runtime.internetworking_model: tcfilter
        # Fix: cgroupv1 im Guest erzwingen — systemd + cgroupv2 blockiert kata-agent exec
        io.katacontainers.config.hypervisor.kernel_params: "systemd.unified_cgroup_hierarchy=0 agent.unified_cgroup_hierarchy=0"
        io.katacontainers.config.runtime.sandbox_cgroup_only: "true"
        # VyOS rootfs ist gross (~400MB) — mehr Zeit fuer VM-Boot + Image-Load
        io.katacontainers.config.runtime.create_container_timeout: "300"
    spec:
      runtimeClassName: kata
      containers:
        - name: router
          image: ghcr.io/berndinox/router:2026.02
          command: ["/sbin/init"]
          securityContext:
            privileged: true
          volumeMounts:
            - name: gitops-config
              mountPath: /opt/gitops-config
              readOnly: true
            - name: gitops-scripts
              mountPath: /opt/gitops-scripts
              readOnly: true
            - name: gitops-secrets
              mountPath: /opt/gitops-secrets
              readOnly: true
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - "nohup /bin/bash /opt/gitops-scripts/config-watcher.sh &>/var/log/config-watcher.log &"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: gitops-config
          configMap:
            name: firewall-config
            items:
              - key: router.conf
                path: router.conf
        - name: gitops-scripts
          configMap:
            name: firewall-watcher
            defaultMode: 0755
        - name: gitops-secrets
          secret:
            secretName: firewall-credentials
