---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # RuntimeClass for Kata Containers (VM-Isolation)
  - runtimeclass.yaml
  # Single macvtap trunk NAD (VyOS handles VLANs internally)
  - nad-trunk.yaml
  # Router
  - sealed-secret.yaml
  - configmap.yaml
  - configmap-watcher.yaml
  - deployment.yaml
