---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # RuntimeClass for Kata Containers (VM-Isolation)
  # CLEANUP-LATER: runtimeclass.yaml entfernen wenn Kata entfernt wird
  - runtimeclass.yaml
  # NAD (nad-trunk.yaml) entfernt — macvlan wird nicht mehr benötigt
  # (Firewall-Pod wird nach MikroTik-Migration ebenfalls entfernt)
  # Router
  - sealed-secret.yaml
  - configmap.yaml
  - configmap-watcher.yaml
  - deployment.yaml
