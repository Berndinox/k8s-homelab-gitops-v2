---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # serviceaccount.yaml entfernt — preUpgradeChecker.jobEnabled=false, kein Chicken-Egg mehr.
  # SA wird jetzt vollständig vom Longhorn Helm-Chart verwaltet.
