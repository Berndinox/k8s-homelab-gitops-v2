---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - application.yaml
  # NADs are NOT here — they need the NetworkAttachmentDefinition CRD from Multus first.
  # NADs are managed by the Multus ArgoCD app itself (kubernetes/core/multus/manifests/)
