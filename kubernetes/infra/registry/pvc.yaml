apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zot-data
  namespace: infra
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi
