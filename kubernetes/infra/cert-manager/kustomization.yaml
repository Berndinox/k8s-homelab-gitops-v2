apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  # ClusterIssuers are managed via extraObjects in helmrelease.yaml
  # clusterissuer.yaml exists as manual fallback — apply after cert-manager is running
