# Talos secrets — generated by talhelper, never commit
talos/clusterconfig/
cluster/talsecret.yaml

# kubeconfig — local only
kubeconfig
kubeconfig-mgmt

# Schematic ID cache
.schematic-id

# SOPS age key — local only, back up separately!
age.key

# Claude Code local settings
.claude/

# Backup-Archive (backup-secrets.sh output)
talos-backup-*.tar.gz.age

# OS artifacts
.DS_Store
Thumbs.db
